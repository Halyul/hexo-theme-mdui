<% page.posts.each(function(post) { %>
    <%-partial('partials/posts', { post: post })%>
<% }); %>

<%-partial('partials/pagination', {base: url_for(config.root) +  'categories/' + page.category + '/', show: theme.index.show })%>
