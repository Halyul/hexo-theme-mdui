<% if(page.layout == 'tags') { %>
	    <!-- Tags Cloud -->
	    <%- partial('widget/page-tagcloud') %>
  <% } else {%>
<article id="page">
  <h1><%= page.title %></h1>
  <%- page.content %>
</article>
<% } %>
<%-partial('partials/pagination')%>
