<% page.posts.each(function(post) { %>
    <%-partial('partials/posts', { post: post })%>
<% }); %>
<%-partial('partials/pagination', {base: url_for(config.root), show: theme.index.show })%>
<%-partial('partials/fab/index')%>
