<script type="text/javascript">
  //index image
  /*(function () {
    var imagesEl = document.getElementsByClassName('theme-posts__post__media');
    Array.prototype.forEach.call(imagesEl, (imageEl) => {
    var img = imageEl.getElementsByClassName('theme-posts__post__media__image')[0]
    var covered = imageEl.getElementsByClassName('theme-index-posts__post__media__covered')[0]
      RGBaster.colors(img, {
        success: function (payload) {
          covered.style.backgroundColor = payload.dominant;
        }
      });
    })
  })();*/
</script>
<script type="text/javascript">
  //index fab
  // BUG: fab wont show after page changed
  (function() {
    var fabTopFlag = false;
    var fabEl = document.getElementById('index-fab');
    function fabShowHide() {
      var bodyScrolled = (document.documentElement && document.documentElement.scrollTop) || document.body.scrollTop;
      if (bodyScrolled > 0 && !fabTopFlag) {
        fabEl.classList.remove('mdui-fab-hide');
        fabTopFlag = true;
      } else if (bodyScrolled === 0 && fabTopFlag) {
        fabEl.classList.add('mdui-fab-hide');
        fabTopFlag = false;
      }
      console.log('sss')
    }
    fabShowHide()
    window.addEventListener('scroll', fabShowHide);
  })();
</script>
