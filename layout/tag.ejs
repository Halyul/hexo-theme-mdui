<% page.posts.each(function(post) { %>
    <%-partial('partials/posts', { post: post })%>
<% }); %>

<%-partial('partials/pagination', {base: url_for(config.root) +  'tags/' + page.tag + '/', show: theme.index.show })%>
