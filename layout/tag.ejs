<% page.posts.each(function(post) { %>
    <%-partial('partials/share/posts', { post: post })%>
<% }); %>

<%-partial('partials/share/pagination', {base: url_for(config.root) +  'tags/' + page.tag + '/', show: theme.index.show })%>

<%-partial('partials/share/fab/index')%>
